# Optional: Only needed for authenticated API access (real trading)
# For public market data (simulation mode), these are NOT required

# BINANCE_API_KEY=your_api_key_here
# BINANCE_API_SECRET=your_secret_here
